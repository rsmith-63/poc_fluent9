import { makeStyles } from "@fluentui/react-components";
import { tokens } from "@fluentui/react-theme";
import { shorthands } from "@griffel/react";

export const adv__styles = makeStyles({
  adv__radioGroup: {
    marginTop: '10px',
  }
});